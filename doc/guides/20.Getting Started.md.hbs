Getting Started
---------

3 steps to be get started

1. [Setup SUGO-Hub](#setup-sugo-hub)
2. [Declare modules on SUGO-Actor](#declare-on-sugo-actor)
3. [Access to modules from SUGO-Caller](#call-from-sugo-caller)

<a id="setup-sugo-hub"></a>
### Setup SUGO-Hub

<a href="{{{links.sugo-hub}}}"><img src="assets/images/sugo-hub-banner.png" alt="banner"
                                      height="{{vars.bannerHeight}}" style="height:{{vars.bannerHeight}}px"
/></a>

Setup a [SUGO-Hub][sugo_hub_url] server for actors and callers.

```javascript
{{{read 'example/example-cloud.js'}}}
```

<a id="declare-on-sugo-actor"></a>
### Declare modules on SUGO-Actor

<a href="{{{links.sugo-actor}}}"><img src="assets/images/sugo-actor-banner.png" alt="banner"
                                     height="{{vars.bannerHeight}}" style="height:{{vars.bannerHeight}}px"
/></a>

Create a [SUGO-Actor][sugo_actor_url] instance and declare modules. Then, connect to the cloud server.

```javascript
{{{read 'example/example-actor.js'}}}
```


<a id="call-from-sugo-caller"></a>
### Access to modules from SUGO-Caller

<a href="{{{links.sugo-caller}}}"><img src="assets/images/sugo-caller-banner.png" alt="banner"
                                         height="{{vars.bannerHeight}}" style="height:{{vars.bannerHeight}}px"
/></a>

Create a [SUGO-Caller][sugo_caller_url] instance and connect to the actor with key.
Then get access to modules and call functions as you like.

```javascript
{{{read 'example/example-caller.js'}}}
```
