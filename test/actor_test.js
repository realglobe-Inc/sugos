/**
 * Test case for actor.
 * Runs with mocha.
 */
'use strict'

const actor = require('../lib/actor.js')
const assert = require('assert')
const co = require('co')

describe('actor', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Spot', () => co(function * () {

  }))
})

/* global describe, before, after, it */
