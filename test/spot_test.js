/**
 * Test case for spot.
 * Runs with mocha.
 */
'use strict'

const spot = require('../lib/spot.js')
const assert = require('assert')
const co = require('co')

describe('spot', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Spot', () => co(function * () {

  }))
})

/* global describe, before, after, it */
